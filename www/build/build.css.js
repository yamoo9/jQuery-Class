({
    // build 디렉토리 기준 설정
    cssIn       : "../css/style.css",
    // build된 CSS 파일 출력 설정
    out         : "../css/style.min.css",
    // 최적화 설정
    optimizeCss : "none", // "none", "standard.(keepLines,keepWhitespace,keepWhitespace)"
})